# Default configuration (used when not running from Jenkins)
userName=hrm_user
password=Hrm_user@123
url=http://localhost:3000/
browser=chrome
environment=grid

# Grid configuration (only used when environment=grid)
seleniumHub=http://localhost:4444/wd/hub

# App URL (can be overridden by Maven)
appUrl=http://3.86.71.8:3000/