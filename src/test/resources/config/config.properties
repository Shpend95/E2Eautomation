browser=chrome
headless=true
websiteAPP=http://54.198.61.50
dbURL=jdbc:mysql://127.0.0.1:3306/mount_hospital
dbUsername=root
dbPassword=Insert123@@

# ENV Options: local, grid, docker-grid, cloud
environment=local
# For LOCAL execution
appUrl=http://54.198.61.50
#(local Selenium Grid)
gridAppUrl=http://54.198.61.50
seleniumHub=http://localhost:4444
# For DOCKER-GRID execution
dockerSeleniumHub=http://localhost:4444
# For CLOUD execution
cloudAppUrl=http://54.198.61.50
cloudSeleniumHub=
## Pattern:
#AppUrl` = **WHERE YOUR APP RUNS** (what you're testing)
#SeleniumHub` = **WHERE SELENIUM GRID RUNS** (test infrastructure)
# SeleniumGrid URL without /wd/hub is just the Grid UI for monitoring and isn't used in code.
gridUrl=http://localhost:4444 should be used
# NOTES FOR QA TEAM
# ========================================
# LOCAL: Tests run on your machine with local browser drivers
# GRID: Tests run on local Selenium Grid (multiple browsers/versions)
# DOCKER-GRID: Tests run on Docker containers (isolated environments)
# CLOUD: Tests run against cloud-deployed application (CI/CD pipeline)